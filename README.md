A `pywinrm` python service establishes a WINRM connection based [`pywinrm`] repo (https://github.com/diyan/pywinrm).

## Installation
```bash
$ pip install -r requirements.txt 
$ chmod +x pywinrm
```

## Usage
```bash
$ ./pywinrm -h
```


